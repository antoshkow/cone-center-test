import React, { FC } from 'react';
import './App.css';
import Input from '../Input/Input';

const App: FC = () => {
  return (
    <div className="app">
      <Input />
    </div>
  );
}

export default App;
